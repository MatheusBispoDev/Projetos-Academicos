import 'package:flutter/material.dart';

class SintomasPage extends StatelessWidget {
  const SintomasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sintomas'),
      ),
    );
  }
}
